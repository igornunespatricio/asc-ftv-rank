# Full API Gateway invoke URL, including the stage name.
# Matches the `api_invoke_url` Terraform output from api_gateway.tf, e.g.:
# https://abc123xyz.execute-api.us-east-1.amazonaws.com/dev
VITE_API_BASE_URL=
